{
  "name": "arabinda07-portfolio",
  "private": true,
  "version": "1.0.0",
  "description": "Smoke tests for static portfolio interactions",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5173",
    "preview": "vite --host 127.0.0.1 --port 4173",
    "test:smoke": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.53.0",
    "vite": "^8.0.14"
  }
}
